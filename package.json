{
  "name": "vice-run-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:debug": "node scripts/dev.js",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "debug-build": "node debug-build.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "gh-pages": "^6.3.0",
    "svelte": "^4.2.20",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "mapbox-gl": "^3.15.0",
    "svg-to-png": "^3.0.0"
  }
}